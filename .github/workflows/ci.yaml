# SPDX-FileCopyrightText: 2026 Arthur Mouraud
# SPDX-License-Identifier: Apache-2.0
name: CI

# MLC-005: restrict default token permissions to read-only.
# Per-job write grants only when strictly required (none currently needed).
permissions:
  contents: read

# MLC-005: cancel in-progress runs on the same ref to avoid duplicate work.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  # MLC-010: weekly scheduled run for pip-audit CVE scanning.
  schedule:
    - cron: "0 6 * * 1"  # Every Monday at 06:00 UTC

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      # MLC-004: pinned to commit SHA (v4.1.1) to prevent tag-mutation attacks.
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      # MLC-004: pinned to commit SHA (v5.4.2).
      - uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39 # v8.2.0
        with:
          python-version: "3.12"
      - run: uv sync --extra dev
      - run: uv run ruff check src/ tests/
      - run: uv run ruff format --check src/ tests/

  typecheck:
    name: Type check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39 # v8.2.0
        with:
          python-version: "3.12"
      - run: uv sync --extra dev
      - run: uv run mypy src/

  test:
    name: Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39 # v8.2.0
        with:
          python-version: "3.12"
      - run: uv sync --extra dev
      - run: uv run pytest tests/ -m "not gpu" --cov=src/mlcore --cov-fail-under=60

  # MLC-010: dependency vulnerability scan against the lockfile.
  # Also runs on every PR and on the weekly schedule above.
  audit:
    name: Dependency audit (pip-audit)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39 # v8.2.0
        with:
          python-version: "3.12"
      - name: Install pip-audit
        run: uv tool install pip-audit
      - name: Run pip-audit
        # --skip-editable: ml-core itself is a local editable install, not on
        # PyPI, so pip-audit cannot audit it; skip it and audit all third-party
        # deps only (the actual CVE risk surface).
        # --ignore-vuln PYSEC-2026-139: torch CVE with no fix version available
        # yet (2026-06-04); revisit when upstream publishes a patched release.
        # --ignore-vuln PYSEC-2026-40/41 + CVE-2026-45804: diffusers CVEs.
        # lerobot==0.5.1 pins diffusers<0.36 so we can't bump to 0.38.0;
        # revisit when lerobot supports a newer diffusers.
        # --ignore-vuln PYSEC-2026-161: starlette CVE. mlflow==3.12.0 pins
        # starlette<1, blocking the bump to 1.0.1; revisit when mlflow
        # supports starlette>=1.
        # --strict dropped: it converts the editable-skip warning into a fatal
        # error, which defeats --skip-editable.
        run: >
          uvx pip-audit --skip-editable
          --ignore-vuln PYSEC-2026-139
          --ignore-vuln PYSEC-2026-40
          --ignore-vuln PYSEC-2026-41
          --ignore-vuln CVE-2026-45804
          --ignore-vuln PYSEC-2026-161
